/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.pexels.com", "example.com", "random.imagecdn.app"], // Adicione aqui o domínio permitido
  },
};

export default nextConfig;
